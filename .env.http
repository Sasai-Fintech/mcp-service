# Sasai Wallet MCP Server Configuration

# HTTP Transport Settings
MCP_HTTP_HOST=localhost
MCP_HTTP_PORT=8000
MCP_HTTP_PATH=/sse

# Transport Type Options: 'sse', 'http', 'streamable-http'
# - sse: Server-Sent Events (recommended for web clients)
# - http: Standard HTTP requests/responses
# - streamable-http: HTTP with streaming support
MCP_TRANSPORT=sse

# Sasai API Configuration (from original settings)
SASAI_BASE_URL=https://sandbox-api.sasai.co.zw/api
SASAI_USERNAME=your_username
SASAI_PASSWORD=your_password
SASAI_PIN=your_pin

# Logging Configuration
LOG_LEVEL=INFO

# Optional: Database URL for analytics features
# DATABASE_URL=mongodb://localhost:27017/sasai_wallet

# Optional: Additional HTTP server settings
# UVICORN_WORKERS=1
# UVICORN_RELOAD=false
